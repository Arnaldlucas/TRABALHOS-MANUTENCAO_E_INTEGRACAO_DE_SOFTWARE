// Este é um hook falso (placeholder) para destravar o DashBoard

export const useSpacedRepetition = () => {
  // Retorna os dados que o DashBoard.tsx espera
  return {
    termosParaRevisar: 5, // Retorna um número falso (ex: 5) para teste
    loading: false,
    error: null,
  };
};